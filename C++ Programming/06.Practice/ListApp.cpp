#include "QUEUE.h"

int main()
{
   // int ListSize = 15;
   // LIST<int> _Queue(ListSize);
   // for (int count = 0; count < ListSize; count++)
   // {
   //     std::cout << _Queue.pushAtBack(count) << "  ";
   // }
   // std::cout << std::endl;
   // _Queue.PrintList();
   // for (int count = 0; count < ListSize; count++)
   // {
   //     std::cout << _Queue.removeFromFront(count) << "  ";
   // }
   // std::cout << std::endl;

    QUEUE<int> Queue1(15);
    //Queue1.SetListSize(12);
    //for (int count = 0; count < 10; count++)
    //{
    //    std::cout << Queue1.enqueue(count) << "\t";
    //}
    //std::cout << std::endl;
    //for (int count = 0; count < 10; count++)
    //{
    //    std::cout << Queue1.dequeue(count) << "\t";
    //}

    return 0;
}